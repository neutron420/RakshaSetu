# Kafka – used by packages that depend on @rakshasetu/kafka (e.g. user-be)
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=rakshasetu
# Set to true to enable; default false (so dev works without Kafka)
KAFKA_ENABLED=false
